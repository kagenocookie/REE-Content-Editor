classes:
  chainsaw.CostumePresetCatalogUserData.Data:
    to_string: "CostumePresetCatalogUserData {_KindID:enum(chainsaw.CharacterKindID)}"
  chainsaw.DropItemCatalogUserData.CatalogItem:
    to_string: "CatalogItem {ID:enum(chainsaw.ItemID)}"
